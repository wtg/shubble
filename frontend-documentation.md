# Shubble Frontend Documentation
Shubble pages (About, Live Location, Schedule) are located within ./cliemt/src/pages. 
Components are located at ./cliemt/src/components
CSS styling is written as classes, not in-line, and are located within ./cliemt/src/styles. This includes styling for all pages and components.

App.tsx renders all pages, and each page renders the components.