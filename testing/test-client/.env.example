# =============================================================================
# TEST CLIENT ENVIRONMENT CONFIGURATION
# =============================================================================

# =============================================================================
# API BASE URL (Optional)
# =============================================================================
# Controls where the test client makes API requests
#
# Option 1: Use Proxy (Default - leave empty)
#   VITE_TEST_BACKEND_URL=
#   - Native: Vite proxies /api/* to http://localhost:4000
#   - Docker: nginx proxies /api/* to http://test-server:4000
#
# Option 2: Direct URLs (Set to test-server URL)
#   VITE_TEST_BACKEND_URL=http://localhost:4000
#   - Makes direct requests to http://localhost:4000/api/*
#   - Requires CORS enabled on test-server (already configured)
#   - Useful for troubleshooting proxy issues

VITE_TEST_BACKEND_URL=

# =============================================================================
# SERVICE URLS - FOR REFERENCE
# =============================================================================
#
# Test Server (Backend API)
#   Native & Docker: http://localhost:4000 (from host)
#                    http://test-server:4000 (from Docker network)
#
# Test Client (This UI)
#   Native & Docker: http://localhost:5174 (Vite dev server / nginx)
#
# =============================================================================
# USAGE EXAMPLES
# =============================================================================
#
# Native with Proxy (Recommended):
#   VITE_TEST_BACKEND_URL=
#   Start: npm run dev
#   Access: http://localhost:5174
#   API calls: /api/* → proxied to → http://localhost:4000/api/*
#
# Native with Direct URLs:
#   VITE_TEST_BACKEND_URL=http://localhost:4000
#   Start: npm run dev
#   Access: http://localhost:5174
#   API calls: http://localhost:4000/api/*
#
# Docker (uses nginx proxy automatically):
#   Environment set by docker-compose.yml
#   Access: http://localhost:5174
#   API calls: /api/* → proxied to → http://test-server:4000/api/*
